<?xml version="1.0" encoding="UTF-8"?>

<!-- Defines the ISO project -->
<iso_project image_name="MAIN.iso">

    <!-- Defines the data track for this tutorial -->
    <track type="data">

        <!-- Specifies identifier strings such as volume label -->
        <!-- System and application identifiers must be PLAYSTATION -->
        <identifiers
            system      ="PLAYSTATION"
            application ="PLAYSTATION"
            volume      ="HERUN"
            volume_set  ="HERUN"
            publisher   ="EMYMIN"
        />

        <!-- Defines the directory tree of the data track -->
        <directory_tree>

            <!-- Specify files in the directory tree -->
            <file name="system.cnf"      type="data" source="../source/system.cnf"/>
            <file name="main.exe"    type="data" source="../source/main.exe"/>

            <!-- Place dummy sectors at the end -->
            <dummy sectors="1024"/>

        </directory_tree>

    </track>

</iso_project>